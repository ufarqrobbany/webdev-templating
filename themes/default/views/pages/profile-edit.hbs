{{!-- File: themes/default/views/pages/profile-edit.hbs --}}
<!DOCTYPE html>
<html lang="id">
<head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Edit Profil</title>
 <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
 <style>
  :root {
      /* Definisikan warna tema Anda agar konsisten */
      --primary-start: #00b4db;
      --primary-end: #0083b0;
      --primary-hover: #0083b0;
      --bg-light: #f6f8fb;
      --bg-input: #f9fafc;
      --card-bg: #fff;
      --text-dark: #333;
      --text-light: #777;
      --border-color: #e0e6ed;
      --avatar-placeholder: #f5a623;
    }

    body {
   font-family: 'Inter', sans-serif;
   background-color: var(--bg-light);
   margin: 0;
   padding: 0;
      color: var(--text-dark);
  }

    /* --- 🎨 PERUBAHAN UI HEADER --- */
  .profile-edit-header {
   background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
   padding: 2.5rem 1rem 6rem 1rem; /* Perbanyak padding bawah */
   text-align: center;
   color: white;
   border-bottom-left-radius: 40px;
   border-bottom-right-radius: 40px;
      position: relative; /* Diperlukan untuk tombol kembali */
  }

    .back-button {
      position: absolute;
      top: 2.5rem; /* Sejajar dengan padding atas header */
      left: 1.5rem;
      transform: translateY(-50%);
      color: white;
      padding: 0.5rem;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background-color 0.2s ease;
      text-decoration: none;
    }
    .back-button:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
    .back-button svg {
      width: 1.5rem;
      height: 1.5rem;
    }

  .profile-edit-header h2 {
   margin: 0;
   font-size: 1.65rem; /* Sedikit lebih besar */
   font-weight: 700; /* Lebih tegas */
  }
    /* --- Akhir Perubahan Header --- */


  .profile-edit-card {
   max-width: 800px;
   background-color: var(--card-bg);
   margin: -4.5rem auto 2rem auto; /* Tarik ke atas sedikit lagi */
   padding: 2rem 2.5rem; /* Sesuaikan padding */
   border-radius: 16px; /* Sedikit lebih besar */
   box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
  }

    /* --- 🎨 PERUBAHAN UI AVATAR --- */
  .profile-avatar {
   display: flex;
   justify-content: center;
   align-items: center;
   flex-direction: column;
   margin-bottom: 2rem; /* Jarak ekstra */
  }

  .profile-avatar-circle {
   width: 110px;
   height: 110px;
   background-color: var(--avatar-placeholder);
   color: white;
   font-size: 2rem;
   font-weight: 700;
   border-radius: 50%;
   display: flex;
   align-items: center;
   justify-content: center;
   border: 4px solid var(--card-bg);
   box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      overflow: hidden; /* Memastikan gambar pas */
  }

    .profile-avatar-circle img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .change-photo-btn {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
   color: var(--primary-end);
   font-size: 0.95rem;
   margin-top: 0.8rem;
   text-decoration: none;
      font-weight: 600;
      padding: 0.4rem 0.8rem;
      border-radius: 8px;
      transition: background-color 0.2s ease;
    }
    .change-photo-btn:hover {
      background-color: var(--bg-input);
    }
    .change-photo-btn svg {
      width: 1rem;
      height: 1rem;
      fill: var(--primary-end);
    }
    /* --- Akhir Perubahan Avatar --- */


    /* --- 🎨 PERUBAHAN UI FORM --- */
    /* Baris untuk 2 kolom */
    .form-row {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 1.25rem; /* Pindahkan margin-bottom ke row */
    }
    .form-row .form-group {
      flex: 1;
      margin-bottom: 0; /* Hapus margin-bottom dari group di dalam row */
    }
    
  .form-group {
   margin-bottom: 1.25rem;
  }

  .form-group label {
   display: block;
   font-weight: 600;
   margin-bottom: 0.5rem;
   color: var(--text-dark);
  }

  .form-group input,
  .form-group textarea {
      box-sizing: border-box; /* Pastikan padding tidak merusak width */
   width: 100%;
   border: 1px solid var(--border-color);
   border-radius: 10px;
   padding: 0.75rem 1rem;
   font-size: 1rem;
   color: var(--text-dark);
   background-color: var(--bg-input);
   transition: border 0.2s ease, box-shadow 0.2s ease; /* Tambah transisi box-shadow */
  }

    .form-group textarea {
      resize: vertical; /* Izinkan resize vertikal saja */
    }

  .form-group input:focus,
  .form-group textarea:focus {
   outline: none;
   border-color: var(--primary-start);
   background-color: var(--card-bg);
      /* Tambahkan glow effect saat focus */
      box-shadow: 0 0 0 3px rgba(0, 180, 219, 0.2);
  }
    /* --- Akhir Perubahan Form --- */


  .form-group small {
   display: block;
   margin-top: 0.25rem;
   color: var(--text-light);
   font-size: 0.85rem;
  }

  .form-actions {
   display: flex;
   justify-content: space-between;
   align-items: center;
   gap: 1rem;
   margin-top: 2rem;
  }

  .btn {
      display: inline-block;
   border: none;
   border-radius: 10px;
   padding: 0.85rem 1.5rem; /* Sedikit lebih tinggi */
   font-size: 1rem;
   cursor: pointer;
   text-align: center;
   font-weight: 600;
      text-decoration: none; /* Untuk link <a> */
   transition: all 0.2s ease;
  }

  .btn:hover {
   transform: translateY(-1px);
      filter: brightness(1.05); /* Efek hover lebih halus */
  }

  .btn-primary {
   flex: 1;
   color: white;
   background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
      box-shadow: 0 2px 8px rgba(0, 131, 176, 0.2); /* Bayangan halus */
  }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(0, 131, 176, 0.3);
    }

  .btn-secondary {
   flex: 1;
   background-color: #f1f3f6;
   color: var(--text-dark);
  }

  .btn-secondary:hover {
   background-color: #e6e9ee;
  }

    /* --- 🎨 PERUBAHAN UI RESPONSIVE --- */
  @media (max-width: 600px) {
      .back-button {
        left: 0.5rem;
        top: 2.2rem;
      }
      .profile-edit-header {
        padding: 2rem 1rem 5rem 1rem;
      }
   .profile-edit-card {
    padding: 2rem 1.5rem;
        margin-left: 1rem;
        margin-right: 1rem;
   }

      /* Buat form-row kembali stacking di mobile */
      .form-row {
        flex-direction: column;
        gap: 1.25rem; /* Tetap beri jarak */
        margin-bottom: 1.25rem;
      }

   .form-actions {
    flex-direction: column;
   }

   .btn {
    width: 100%;
        box-sizing: border-box; /* Pastikan padding tidak merusak */
   }
  }
 </style>
</head>
<body>

 <header class="profile-edit-header">
    {{!-- Tombol Kembali Baru --}}
    <a href="/api/v1/auth/profile" class="back-button" aria-label="Kembali">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="24" height="24">
        <path fill-rule="evenodd" d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z" clip-rule="evenodd" />
      </svg>
    </a>
  <h2>Edit Profil</h2>
 </header>

 <main class="profile-edit-card">

  <div class="profile-avatar">
   <div class="profile-avatar-circle">
    {{#if user.avatar}}
     <img src="{{user.avatar}}" alt="Avatar">
    {{else}}
     {{user.initials}}
    {{/if}}
   </div>
      {{!-- Link Ganti Foto Baru dengan Ikon --}}
   <a href="/api/v1/auth/profile/photo" class="change-photo-btn">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="16" height="16">
          <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z" />
        </svg>
        <span>Ganti Foto Profil</span>
      </a>
  </div>

  <form action="/api/v1/auth/profile" method="POST">
      {{!-- Layout Baris Form Baru --}}
      <div class="form-row">
     <div class="form-group">
      <label for="firstName">Nama Depan</label>
      <input type="text" id="firstName" name="firstName" value="{{user.firstName}}" required>
     </div>

     <div class="form-group">
      <label for="lastName">Nama Belakang</label>
      <input type="text" id="lastName" name="lastName" value="{{user.lastName}}" required>
     </div>
      </div>
      {{!-- Akhir Layout Baris Form --}}

   <div class="form-group">
    <label for="bio">Bio</label>
    <textarea id="bio" name="bio" maxlength="150" rows="3">{{user.bio}}</textarea>
    <small>Maksimal 150 karakter</small>
   </div>

   <div class="form-actions">
    <a href="/api/v1/auth/profile" class="btn btn-secondary">Batal</a>
    <button type="submit" class="btn btn-primary">Simpan Perubahan</button>
   </div>
  </form>

 </main>

</body>
</html>