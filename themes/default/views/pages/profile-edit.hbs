{{!-- File: themes/default/views/pages/profile-edit.hbs --}}
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profil</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      /* Definisikan warna tema Anda agar konsisten */
      --primary-start: #00b4db;
      --primary-end: #0083b0;
      --primary-hover: #0083b0;
      --bg-light: #f6f8fb;
      --bg-input: #f9fafc;
      --card-bg: #fff;
      --text-dark: #333;
      --text-light: #777;
      --border-color: #e0e6ed;
      --avatar-placeholder: #f5a623;
      /* Warna baru untuk notifikasi sukses (opsional, bisa pakai primary) */
      --success-border: #4CAF50;
      --success-bg: #f6fffb;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg-light);
      margin: 0;
      padding: 0;
      color: var(--text-dark);
    }

    /* --- 🎨 UI HEADER --- */
    .profile-edit-header {
      background:
        linear-gradient(135deg, rgba(255,255,255,0.85) 0%, rgba(255,255,255,0.65) 40%, rgba(255,255,255,0.45) 100%),
        linear-gradient(135deg, #0a3e8c 0%, #2563eb 45%, #3b82f6 100%);
      padding: 4rem 1rem 6rem 1rem;
      text-align: center;
      color: white;
      border-bottom-left-radius: 40px;
      border-bottom-right-radius: 40px;
      position: relative;
      z-index: 1;
    }

    .back-button {
      position: absolute;
      top: 2.5rem; 
      left: 1.5rem;
      transform: translateY(-50%);
      color: white;
      padding: 0.5rem;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background-color 0.2s ease;
      text-decoration: none;
    }
    .back-button:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }
    .back-button svg {
      width: 1.5rem;
      height: 1.5rem;
    }

    .profile-edit-header h2 {
      margin: 0;
      font-size: 1.65rem; 
      font-weight: 700; 
    }
    /* --- Akhir Header --- */


    .profile-edit-card {
      max-width: 800px;
      background-color: var(--card-bg);
      margin: -4.5rem auto 2rem auto; 
      padding: 2rem 2.5rem; 
      border-radius: 16px; 
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
      position: relative;
      z-index: 2;
    }

    /* --- 🎨 UI AVATAR --- */
    .profile-avatar {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      margin-bottom: 2rem; 
    }

    .profile-avatar-circle {
      width: 110px;
      height: 110px;
      background-color: var(--avatar-placeholder);
      color: white;
      font-size: 2rem;
      font-weight: 700;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 4px solid var(--card-bg);
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      overflow: hidden; 
    }

    .profile-avatar-circle img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .change-photo-btn {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      color: var(--primary-end);
      font-size: 0.95rem;
      margin-top: 0.8rem;
      text-decoration: none;
      font-weight: 600;
      padding: 0.4rem 0.8rem;
      border-radius: 8px;
      transition: background-color 0.2s ease;
      cursor: pointer; 
    }
    .change-photo-btn:hover {
      background-color: var(--bg-input);
    }
    .change-photo-btn svg {
      width: 1rem;
      height: 1rem;
      fill: var(--primary-end);
    }
    /* --- Akhir Avatar --- */


    /* --- 🎨 UI FORM --- */
    .form-row {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 1.25rem; 
    }
    .form-row .form-group {
      flex: 1;
      margin-bottom: 0; 
    }
    
    .form-group {
      margin-bottom: 1.25rem;
    }

    .form-group label {
      display: block;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-dark);
    }

    .form-group input,
    .form-group textarea {
      box-sizing: border-box; 
      width: 100%;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      color: var(--text-dark);
      background-color: var(--bg-input);
      transition: border 0.2s ease, box-shadow 0.2s ease; 
    }

    .form-group textarea {
      resize: vertical; 
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--primary-start);
      background-color: var(--card-bg);
      box-shadow: 0 0 0 3px rgba(0, 180, 219, 0.2);
    }
    /* --- Akhir Form --- */


    .form-group small {
      display: block;
      margin-top: 0.25rem;
      color: var(--text-light);
      font-size: 0.85rem;
    }

    .form-actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      margin-top: 2rem;
    }

    .btn {
      display: inline-block;
      border: none;
      border-radius: 10px;
      padding: 0.85rem 1.5rem; 
      font-size: 1rem;
      cursor: pointer;
      text-align: center;
      font-weight: 600;
      text-decoration: none; 
      transition: all 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
      filter: brightness(1.05); 
    }

    .btn-primary {
      flex: 1;
      color: white;
      background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
      box-shadow: 0 2px 8px rgba(0, 131, 176, 0.2); 
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(0, 131, 176, 0.3);
    }

    .btn-secondary {
      flex: 1;
      background-color: #f1f3f6;
      color: var(--text-dark);
    }

    .btn-secondary:hover {
      background-color: #e6e9ee;
    }

    /* --- 🎨 UI RESPONSIVE --- */
    @media (max-width: 600px) {
      .back-button {
        left: 0.5rem;
        top: 2.2rem;
      }
      .profile-edit-header {
        padding: 2rem 1rem 5rem 1rem;
      }
      .profile-edit-card {
        padding: 2rem 1.5rem;
        margin-left: 1rem;
        margin-right: 1rem;
      }

      .form-row {
        flex-direction: column;
        gap: 1.25rem; 
        margin-bottom: 1.25rem;
      }

      .form-actions {
        flex-direction: column;
      }

      .btn {
        width: 100%;
        box-sizing: border-box; 
      }
    }

    /* ============================================= */
    /* --- 🎨 CSS BARU: TOAST NOTIFIKASI ELEGAN --- */
    /* ============================================= */
    #elegant-toast {
      visibility: hidden;
      opacity: 0;
      position: fixed;
      bottom: -100px; /* Mulai dari bawah */
      left: 50%;
      transform: translateX(-50%);
      
      display: flex;
      align-items: center;
      gap: 1rem;
      
      background-color: var(--card-bg);
      color: var(--text-dark);
      font-weight: 600;
      
      border: 1px solid var(--border-color);
      /* Aksen biru sesuai tema */
      border-left: 5px solid var(--primary-start); 
      
      border-radius: 10px;
      padding: 16px 24px;
      z-index: 9999;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
      
      /* Transisi halus */
      transition: all 0.4s ease-in-out; 
    }

    #elegant-toast .toast-icon {
      width: 24px;
      height: 24px;
      /* Warna ikon checklist sesuai aksen */
      fill: var(--primary-start); 
    }

    /* Kelas .show ditambahkan oleh JS */
    #elegant-toast.show {
      visibility: visible;
      opacity: 1;
      bottom: 30px; /* Muncul ke posisi ini */
    }

  </style>
</head>
<body>

  <header class="profile-edit-header">
    <a href="/users/{{user.id}}/profile" class="back-button" aria-label="Kembali">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="24" height="24">
        <path fill-rule="evenodd" d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z" clip-rule="evenodd" />
      </svg>
    </a>
    <h2>Edit Profil</h2>
  </header>

  <main class="profile-edit-card">

    <div class="profile-avatar">
      <div class="profile-avatar-circle">
        <img 
          id="avatar-preview" 
          src="{{#if user.photo.path}}{{user.photo.path}}{{else}}/path/to/default-avatar.png{{/if}}" 
          alt="Avatar"
        >
      </div>

      <input type="file" id="file-upload-input" accept="image/*" style="display: none;">
      
      <label for="file-upload-input" class="change-photo-btn">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" width="16" height="16">
              <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z" />
          </svg>
          <span>Ganti Foto Profil</span>
      </label>
      
      <small id="upload-status" style="margin-top: 8px; color: var(--text-light);"></small>
    </div>

    <form id="profile-form">
      
      <input type="hidden" id="photoId" name="photo[id]" value="{{user.photo.id}}">

      <div class="form-row">
        <div class="form-group">
          <label for="firstName">Nama Depan</label>
          <input type="text" id="firstName" name="firstName" value="{{user.firstName}}" required>
        </div>

        <div class="form-group">
          <label for="lastName">Nama Belakang</label>
          <input type="text" id="lastName" name="lastName" value="{{user.lastName}}" required>
        </div>
      </div>

      <div class="form-group">
        <label for="bio">Bio</label>
        <textarea id="bio" name="bio" maxlength="150" rows="3">{{user.bio}}</textarea>
        <small>Maksimal 150 karakter</small>
      </div>

      <div class="form-actions">
        {{!-- Tombol Batal sudah diperbaiki --}}
        <a href="/users/{{user.id}}/profile" class="btn btn-secondary">Batal</a>
        <button type="submit" class="btn btn-primary">Simpan Perubahan</button>
      </div>
    </form>
  </main>


  <div id="elegant-toast">
    <svg class="toast-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
      <path fill-rule="evenodd" d="M2.25 12c0-5.385 4.365-9.75 9.75-9.75s9.75 4.365 9.75 9.75-4.365 9.75-9.75 9.75S2.25 17.385 2.25 12zm13.36-1.814a.75.75 0 10-1.22-.872l-3.236 4.53L9.53 12.22a.75.75 0 00-1.06 1.06l2.25 2.25a.75.75 0 001.14-.094l3.75-5.25z" clip-rule="evenodd" />
    </svg>
    <span id="toast-message"></span>
  </div>


  <script>
    document.addEventListener('DOMContentLoaded', () => {

      const fileInput = document.getElementById('file-upload-input');
      const photoIdInput = document.getElementById('photoId');
      const profileForm = document.getElementById('profile-form');
      const avatarPreview = document.getElementById('avatar-preview');
      const uploadStatus = document.getElementById('upload-status');
      
      // GANTI DENGAN TOKEN ASLI
      const AUTH_TOKEN = 'Bearer YOUR_AUTH_TOKEN_HERE'; 

      // ==========================================================
      // FUNGSI BARU: NAMPILIN TOAST ELEGAN
      // ==========================================================
      function showElegantToast(message) {
        const toast = document.getElementById('elegant-toast');
        const toastMessage = document.getElementById('toast-message');
        if (!toast || !toastMessage) return; 

        toastMessage.textContent = message;
        toast.classList.add('show');
      }

      // ==========================================================
      // STEP 1 & 2: UPLOAD FOTO (Tidak Berubah)
      // ==========================================================
      fileInput.addEventListener('change', async (event) => {
        const file = event.target.files[0];
        if (!file) return;

        avatarPreview.src = URL.createObjectURL(file);
        uploadStatus.textContent = 'Mengupload foto...';

        const formData = new FormData();
        formData.append('file', file);

        try {
          const response = await fetch('/api/v1/files/upload', {
            method: 'POST',
            body: formData,
            headers: { 'Authorization': AUTH_TOKEN }
          });

          if (!response.ok) {
            const errorData = await response.json();
            throw new Error(errorData.message || 'Upload gagal');
          }

          const result = await response.json();
          photoIdInput.value = result.file.id;
          uploadStatus.textContent = 'Foto berhasil diupload!';

        } catch (error) {
          console.error('Error uploading file:', error);
          uploadStatus.textContent = `Upload gagal: ${error.message}`;
          avatarPreview.src = "{{#if user.photo.path}}{{user.photo.path}}{{else}}/path/to/default-avatar.png{{/if}}"; 
        }
      });


      // ==========================================================
      // STEP 3: SIMPAN PROFIL (ALERT DIGANTI TOAST)
      // ==========================================================
      profileForm.addEventListener('submit', async (event) => {
        event.preventDefault(); 

        let photoId = photoIdInput.value;
        if (photoId === 'undefined' || photoId === '') {
          photoId = null; 
        }

        const data = {
          firstName: document.getElementById('firstName').value,
          lastName: document.getElementById('lastName').value,
          bio: document.getElementById('bio').value
        };

        if (photoId) {
          data.photo = { id: photoId };
        }

        try {
          const response = await fetch('/api/v1/auth/me', {
            method: 'PATCH',
            headers: {
              'Content-Type': 'application/json',
              'Authorization': AUTH_TOKEN
            },
            body: JSON.stringify(data) 
          });

          if (!response.ok) {
            const errorData = await response.json();
            throw new Error(errorData.message || 'Gagal menyimpan profil');
          }

          // =============================================
          // --- 🎨 PERUBAHAN ALERT KE TOAST DI SINI 🎨 ---
          // =============================================
          
          // 6. Sukses! Panggil toast
          showElegantToast('Profil berhasil diperbarui.');

          // 7. Tunggu 2 detik baru redirect, biar user sempet baca
          setTimeout(() => {
            window.location.href = '/users/{{user.id}}/profile';
          }, 2000); // 2 detik

        } catch (error) {
          console.error('Error updating profile:', error);
          // Biarkan error tetap pakai alert standar
          alert(`Gagal menyimpan profil: ${error.message}`);
        }
      });
    });
  </script>
</body>
</html>